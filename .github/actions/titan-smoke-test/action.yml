name: Titan smoke test
description: >
  Run the image's built-in entrypoint against `--help` and `dashboard --help`
  to catch basic runtime regressions before publishing.  Requires the image
  to already be loaded into the local Docker daemon under `image`.

  Works identically on amd64 and arm64 runners.

inputs:
  image:
    description: Fully-qualified image tag (e.g. nousresearch/titan-agent:test)
    required: true

runs:
  using: composite
  steps:
    - name: Ensure /tmp/Titan-test is Titan-writable
      shell: bash
      run: |
        # The image runs as the Titan user (UID 10000).  GitHub Actions
        # creates /tmp/Titan-test root-owned by default, which Titan
        # can't write to — chown it to match the in-container UID before
        # bind-mounting.  Real users doing `docker run -v ~/.Titan:...`
        # with their own UID hit the same issue and have their own
        # remediations (Titan_UID env var, or chown locally).
        mkdir -p /tmp/Titan-test
        sudo chown -R 10000:10000 /tmp/Titan-test

    - name: Titan --help
      shell: bash
      run: |
        docker run --rm \
          -v /tmp/Titan-test:/opt/data \
          --entrypoint /opt/Titan/docker/entrypoint.sh \
          "${{ inputs.image }}" --help

    - name: Titan dashboard --help
      shell: bash
      run: |
        # Regression guard for #9153: dashboard was present in source but
        # missing from the published image.  If this fails, something in
        # the Dockerfile is excluding the dashboard subcommand from the
        # installed package.
        docker run --rm \
          -v /tmp/Titan-test:/opt/data \
          --entrypoint /opt/Titan/docker/entrypoint.sh \
          "${{ inputs.image }}" dashboard --help

